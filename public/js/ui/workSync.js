export function renderWorkSync(root) {
  root.innerHTML = `
    <section class="panel">
      <h2>Work & Class Sync</h2>
      <p>We will check your shifts against your classes and warn about conflicts.</p>
      <ul>
        <li>No work/class conflicts detected (sample).</li>
      </ul>
    </section>
  `;
}
